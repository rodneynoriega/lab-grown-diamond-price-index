__pycache__/
*.py[cod]
.DS_Store
.venv/
venv/
*.egg-info/
data/raw/
data/processed/
# listing-level snapshots live in a separate PRIVATE repo (nested git repo
# rooted at data/snapshots/) -- never commit row-level data to this public repo
data/snapshots/

# local credentials
.env

# pilot batch rollback manifests + body stores: page bodies carry
# listing-level data -- never commit to this public repo
manifests/
# kill-switch / drill run logs
kill-switch-*.log
# generated pilot page bodies + records: listing-level data, never in
# the public repo (same rule as manifests/)
page_output/
